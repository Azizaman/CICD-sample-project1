# CI usually does:

#1) Install dependencies

#2) Build the app

#3) Run tests

#4) Lint / checks

# 📌 CI never deploys


name: Task 3 CI workflow

on:
    workflow_dispatch:
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            #checkout the code
            - name: checkout the code
              uses: actions/checkout@v4
            - name: set up the node js envirnment
              uses: actions/setup-node@v3
            - name: install dependecies
              run: npm install
            - name: install jest
              run: npm install --save-dev jest
            - name: run the tests
              run: npm test
            - name: lint the code
              run: npx eslint --init
            - name: run the lint
              run: npm run lint

# CD usually does:

# Build Docker image

# Push image

# Deploy to server / cloud

# 📌 CD should be controlled and restricted
# via protected branches or manual approvals
# 📌 CD never runs tests
        steps:
            -name: 



